apiVersion: v1
kind: Service
metadata:
  name: nginx
  # labels:
  #   app: nginx
  #   tier: frontend
spec:
  type: NodePort
  ports:
    - targetPort: 80
      port: 80
      nodePort: 30008
  selector:
    app: myapp
    type: frontend